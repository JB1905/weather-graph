import { withSuspense } from 'react-suspenser';
// import { compose } from 'recompose';

// TODO
export const withDynamicImport = withSuspense();
// export const dynamic = withSuspense();
// export const withDynamicImport = compose(withSuspense());
