{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "//TODO": "watch -> start, start -> serve",
    "start": "node dist/server.js",
    "build": "tsc",
    "watch": "nodemon --watch 'src/**/*.ts' --exec ts-node src/server.ts"
  },
  "dependencies": {
    "apollo-server": "^2.18.1",
    "dotenv": "^8.2.0",
    "graphql": "^15.3.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "@types/node-fetch": "^2.5.7",
    "husky": "^4.3.0",
    "lint-staged": "^10.4.0",
    "nodemon": "^2.0.4",
    "prettier": "^2.1.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
