{
  "version": 2,
  "env": {
    "WEATHER_API_KEY": "@weather-api-key"
  },
  "builds": [{ "src": "src/index.ts", "use": "@now/node" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}
